# Copy this file to .env and fill in real values (same Supabase project as
# the web app). This file uses the SECRET/service_role key -- it needs full
# write access, unlike the web app's public anon key.

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-secret-key
